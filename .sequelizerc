const path = require('path');

module.exports = {
  config: path.resolve(__dirname, 'src', 'config', 'database', 'postgres', 'config.ts'),
  'migrations-path': path.resolve(__dirname, 'src', 'database', 'migrations'),
};
